apiVersion: v1
kind: Service
metadata:
  name: pico-placa-service
  labels:
    app: pico-placa
spec:
  type: NodePort
  selector:
    app: pico-placa
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
